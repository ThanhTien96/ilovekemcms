export default () => ({
    documentation: {
        enabled: true,
        config: {
          "x-strapi-config": {
            // Default
            plugins: ["upload"],
            // Custom
          },
        },
      },
});
